PHASE=2.5

# Phase 2 — Complete (approved). Recurring lessons, schedule, validation — see devlog-phase2.md.

# Phase 2.5 — Current: LLM «שאלה למאמן» (students ask the coach via OpenAI-compatible API).
#   • Code: POST /api/player/ask, /player/ask — see arch-phase2-5-llm.md, devlog-phase2-5.md
#   • Config: OPENAI_API_KEY in .env.local — see docs/env-llm.md and .env.example

# Phase 3 — Next: Morning payment integration — see manager-phase3.md, arch-phase3.md
